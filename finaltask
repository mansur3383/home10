# Задача 44:
# В ячейке ниже представлен код генерирующий DataFrame, которая состоит всего из 1 столбца. 
# Ваша задача перевести его в one hot вид. 
# Сможете ли вы это сделать без get_dummies?

импортируйте панд как pd 
импортируйте numpy как np 
импорт случайным образом
 
= lst ['робот'] * 10
+= lst ['человек'] * 10
случайный.перемешивание(первое)
pd = data.Фрейм данных({'whoAmI': lst})
печать(данных)
 
печать(")

данные['tmp'] = 1
data.set_index([data.index, 'whoAmI'], inplace=True)
data = data.unstack(уровень=-1, fill_value = 0).astype(int)
данные.столбцы = data.columns.droplevel()
данные.столбцы.имя = Нет
печать(данных)
